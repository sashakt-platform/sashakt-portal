import type { languageOption } from './types/test';

export const PASSWORD_MIN = 8;
export const DEFAULT_PAGE_SIZE = 25;
export const DEFAULT_LANGUAGE: languageOption = { language: 'English', code: 'en-US' };
